{
  "version": 2,
  "buildCommand": "chmod +x node_modules/.bin/* && cd clinic-app && npm run build",
  "installCommand": "npm install && chmod +x node_modules/.bin/*",
  "outputDirectory": "clinic-app/dist"
}
